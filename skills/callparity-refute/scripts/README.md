# scripts

This skill does not execute host scripts during a live CALL-E turn.

Host helpers live in the CallParity repo root: scripts/seed_demo_data.py (idempotent seed of FR-1842, FR-1900, FR-1888).

Keep this folder so the awesome-phone-call-agents skill template (SKILL.md + references/ + scripts/) is complete. Add planner dry-run utilities here later; do not shell out from the model turn.
