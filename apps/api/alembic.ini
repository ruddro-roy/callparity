# Alembic configuration for the CallParity API schema.
#
# The database URL is not stored here. alembic/env.py resolves it at runtime
# from the application settings (DATABASE_URL / .env), so the CLI and the app
# always migrate the same database:
#
#     DATABASE_URL=... alembic -c apps/api/alembic.ini upgrade head

[alembic]
script_location = %(here)s/alembic

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
